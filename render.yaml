services:
  - type: web
    name: fake-news-detector
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT wsgi:application --workers 4 --timeout 120
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0